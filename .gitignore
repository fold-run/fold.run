node_modules/
dist/
.astro/
.wrangler/
.DS_Store

# stardust redesign pipeline artifacts (local record, not source)
stardust/

# agent working directories (local record, not source)
.claude/
.impeccable/
