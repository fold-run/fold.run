{
  "name": "@fold-run/site",
  "version": "0.0.1",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && wrangler dev",
    "typecheck": "tsc --noEmit",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "wrangler": "catalog:"
  }
}
