---
// fold.run home — stardust redesign (stardust/migrated/index.html), transplanted
// onto the shared Base layout. Markup and copy are the approved migrated design.
import Base from '../layouts/Base.astro';

const description =
  'One governed endpoint between every MCP client and every MCP server. Federation, auth, policy, caching, and audit — built on the official MCP Go SDK.';

const jsonld = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'Organization',
      name: 'fold',
      url: 'https://fold.run/',
      logo: 'https://fold.run/favicon.svg',
      sameAs: ['https://github.com/fold-run/fold'],
    },
    { '@type': 'WebSite', name: 'fold', url: 'https://fold.run/' },
  ],
};
---

<Base
  title="fold — the enterprise MCP gateway"
  description={description}
  canonical="https://fold.run/"
  jsonld={jsonld}
  template="landing"
>
  <main id="main">
    <section
      class="hero"
      data-glow="true"
      data-section="hero"
      data-intent="value proposition + show the topology"
      data-layout="split-media"
      data-media="image"
    >
      <!-- pattern-paper ground: a ruled sheet that lights under the pointer.
           Nothing moves on its own — the only motion is the visitor's own.
           Decorative. -->
      <div class="hero-grid" aria-hidden="true">
        <div class="hero-rules"></div>
        <div class="hero-cursor"></div>
      </div>
      <div class="hero-field" aria-hidden="true">
        <span class="glow glow-a"></span>
        <span class="glow glow-b"></span>
      </div>
      <div class="wrap">
        <div>
          <!-- Separators are spaced by CSS, not by source whitespace: Astro
               collapses the newlines between inline elements and the marks
               end up fused to the words. -->
          <p class="kicker"><a href="https://github.com/fold-run/fold/releases">v1.5.0</a><span
              class="sep"
              aria-hidden="true">·</span
            >Apache-2.0<span class="sep" aria-hidden="true">·</span><a
              href="https://docs.fold.run/conformance/">40/40 MCP conformant</a
            ></p>
          <h1>The enterprise MCP gateway.</h1>
          <p class="lede">
            One governed endpoint between every MCP (Model Context Protocol) client and every MCP
            server. Federation, auth, policy, caching, and audit — built on the official MCP Go SDK
            on both sides of the proxy.
          </p>
          <div class="cta-row">
            <a class="btn btn-primary btn-live" href="https://docs.fold.run/getting-started/">Get started</a>
            <a class="btn btn-secondary" href="https://github.com/fold-run/fold">View source</a>
          </div>
        </div>
        <div class="diagram-panel">
          <!-- authored topology diagram: fold-line vocabulary (tailor's pattern paper) -->
          <svg
            viewBox="0 0 560 400"
            role="img"
            aria-label="Three MCP clients connect to one fold gateway, which applies auth, policy, and audit, and federates three upstream servers: cfdocs, git, and jobs."
          >
            <!-- dashed fold-lines: clients into the gateway -->
            <path class="fold-dash" d="M 118 80  C 200 80, 200 176, 246 184"></path>
            <path class="fold-dash" d="M 118 328 C 200 328, 200 224, 246 216"></path>
            <!-- active route: middle client -> fold -> git upstream -->
            <path class="fold-active draw" d="M 118 200 H 246 M 314 200 C 360 200, 380 200, 442 200"
            ></path>
            <!-- grain arrow on the active route -->
            <path class="fold-arrow" d="M 432 194 L 444 200 L 432 206 Z"></path>
            <!-- dashed fold-lines: gateway out to upstreams -->
            <path class="fold-dash" d="M 314 184 C 370 176, 380 96, 442 88"></path>
            <path class="fold-dash" d="M 314 216 C 370 224, 380 304, 442 312"></path>
            <!-- notch marks at the governed boundary (auth · policy · audit) -->
            <path class="fold-notch" d="M 236 192 v 16 M 240 190 v 20"></path>
            <path class="fold-notch" d="M 320 190 v 20 M 324 192 v 16"></path>
            <!-- client nodes -->
            <g>
              <rect class="fold-node" x="30" y="62" width="88" height="36" rx="2"></rect>
              <rect class="fold-node" x="30" y="182" width="88" height="36" rx="2"></rect>
              <rect class="fold-node" x="30" y="310" width="88" height="36" rx="2"></rect>
              <text class="fold-node-label" x="74" y="84" text-anchor="middle">client</text>
              <text class="fold-node-label" x="74" y="204" text-anchor="middle">client</text>
              <text class="fold-node-label" x="74" y="332" text-anchor="middle">client</text>
            </g>
            <!-- fold gateway node with folded corner -->
            <g>
              <rect
                x="246"
                y="168"
                width="68"
                height="64"
                rx="2"
                fill="#1a1a1a"
                stroke="#D6FF00"
                stroke-width="1.5"></rect>
              <path d="M 314 186 V 176 A 2 2 0 0 0 306 168 H 296 Z" fill="#D6FF00"></path>
              <text class="fold-node-label" x="280" y="204" text-anchor="middle" font-weight="700"
                >fold</text
              >
            </g>
            <!-- upstream nodes: captured demo prefixes -->
            <g>
              <rect class="fold-node" x="434" y="70" width="112" height="36" rx="2"></rect>
              <rect class="fold-node" x="434" y="182" width="112" height="36" rx="2"></rect>
              <rect class="fold-node" x="434" y="294" width="112" height="36" rx="2"></rect>
              <text class="fold-node-label" x="490" y="92" text-anchor="middle">cfdocs__*</text>
              <text class="fold-node-label" x="490" y="204" text-anchor="middle">git__*</text>
              <text class="fold-node-label" x="490" y="316" text-anchor="middle">jobs__*</text>
            </g>
            <!-- annotations: measurement marks, pattern-paper style -->
            <text class="fold-anno" x="280" y="156" text-anchor="middle">auth · policy · audit</text>
            <text class="fold-anno" x="280" y="252" text-anchor="middle"
              >one governed endpoint · /mcp</text
            >
            <text class="fold-anno" x="80" y="52" text-anchor="middle">any MCP client</text>
            <text class="fold-anno" x="470" y="60" text-anchor="middle">upstreams</text>
          </svg>
        </div>
      </div>
    </section>

    <section
      class="proof"
      data-section="proof-band"
      data-intent="build trust with receipts"
      data-layout="grid"
      data-items="4"
      data-module="proof-band"
    >
      <div class="wrap">
        <a class="stat" href="https://docs.fold.run/conformance/">
          <b>40/40</b>
          <span>official MCP conformance checks, run through the gateway on every merge</span>
        </a>
        <a class="stat" href="https://docs.fold.run/benchmarks/">
          <b>~0.2 ms</b>
          <span
            >typical added p50 latency (Apple Silicon, in-process upstream); CI gates every merge at
            &lt; 5 ms</span
          >
        </a>
        <a class="stat" href="https://github.com/fold-run/fold/releases">
          <b>1 binary</b>
          <span>static Go binary or ~22 MB distroless container, no local state</span>
        </a>
        <a class="stat" href="https://github.com/fold-run/fold">
          <b>Apache-2.0</b>
          <span>open source, on GitHub</span>
        </a>
      </div>
    </section>

    <section
      class="demo"
      id="demo"
      data-section="demo"
      data-intent="prove it live"
      data-layout="contained"
      data-items="3"
    >
      <div class="wrap">
        <div class="section-head">
          <h2>See it live.</h2>
          <p class="intro">
            demo.fold.run federates three public MCP servers behind one endpoint — the unmodified
            release binary, governed by its own config. Point any MCP client at it. Rate-limited,
            unauthenticated, no warranty; live status on <a href="/status/">/status</a>. Copy-paste
            walkthrough: <a href="https://docs.fold.run/try-the-demo/">try the demo</a>.
          </p>
        </div>
        <div class="route-rail">
          <span class="chip">demo.fold.run/mcp</span>
          <span class="muted" style="font-size: var(--body-sm)">three upstreams · one endpoint</span
          >
        </div>
        <div class="termini">
          <article class="terminus">
            <h3>cfdocs__*</h3>
            <p>
              Cloudflare's public docs MCP server — a real third-party upstream, governed and
              namespaced like any internal one.
            </p>
          </article>
          <article class="terminus">
            <h3>git__*</h3>
            <p>
              GitMCP — a public 2025-era server on the session handshake. Behind the gateway it is
              just another namespace.
            </p>
          </article>
          <article class="terminus">
            <h3>jobs__*</h3>
            <p>
              A task-minting demo server. Start a job, then poll it with nothing but the task id —
              fold routes every poll to the owner.
            </p>
          </article>
        </div>
      </div>
    </section>

    <section
      class="runit"
      id="run"
      data-section="run-it"
      data-intent="drive action"
      data-layout="contained"
      data-items="3"
    >
      <div class="wrap">
        <div class="section-head">
          <h2>Run it in 60 seconds.</h2>
          <p class="intro">
            fold is a single static binary with no local state. One config file in front of your
            first server, governance when you're ready.
          </p>
        </div>

        <ol class="route-steps">
          <li class="step">
            <div class="marker" aria-hidden="true">1</div>
            <div>
              <h3>Describe your upstreams</h3>
              <div class="code-panel">
                <div class="tag">
                  <span>fold.config.json</span><button
                    type="button"
                    data-copy
                    aria-label="Copy fold.config.json">copy</button
                  >
                </div>
                <pre
                  tabindex="0"
                  role="region"
                  aria-label="fold.config.json contents"><code>&#123;
  <span class="k">"upstreams"</span>: [
    &#123;
      <span class="k">"id"</span>: "github-tools",
      <span class="k">"url"</span>: "https://mcp.platform.acme.com/mcp",
      <span class="k">"namespace"</span>: "gh",
      <span class="k">"owner"</span>: &#123; <span class="k">"org"</span>: "acme-platform", <span class="k">"team"</span>: "devex" &#125;
    &#125;
  ]
&#125;</code></pre>
              </div>
            </div>
          </li>

          <li class="step">
            <div class="marker" aria-hidden="true">2</div>
            <div>
              <h3>Run it</h3>
              <div class="code-panel">
                <div class="tag">
                  <span>shell</span><button type="button" data-copy aria-label="Copy run commands"
                    >copy</button
                  >
                </div>
                <pre
                  tabindex="0"
                  role="region"
                  aria-label="Run commands"><code><span class="c"># one upstream, one governed endpoint — 60 seconds</span>
<span class="k">$</span> go run github.com/fold-run/fold/cmd/fold@latest --config fold.config.json
<span class="c"># or the container:</span>
<span class="k">$</span> docker run -p 8080:8080 -e FOLD_CONFIG="$(cat fold.config.json)" ghcr.io/fold-run/fold</code></pre>
              </div>
            </div>
          </li>

          <li class="step">
            <div class="marker" aria-hidden="true">3</div>
            <div>
              <h3>Point a client at it</h3>
              <p class="muted">
                Any MCP client connects to <span class="mono">/mcp</span> and sees one virtual
                server named fold with every team's tools. <span class="mono"
                  >MCP endpoint: http://localhost:8080/mcp · health: /health · metrics: /metrics</span
                >
              </p>
            </div>
          </li>
        </ol>

        <div class="paths">
          <article class="path">
            <h3>Binary</h3>
            <p>
              A single static Go binary — go install, or grab a prebuilt archive from GitHub
              releases. Linux and macOS, amd64 and arm64.
              <span class="cmd">go install github.com/fold-run/fold/cmd/fold@latest</span>
            </p>
          </article>
          <article class="path">
            <h3>Container</h3>
            <p>
              Multi-arch and distroless, ~22 MB. Config injected as a file or straight through
              FOLD_CONFIG.
              <span class="cmd">ghcr.io/fold-run/fold</span>
            </p>
          </article>
          <article class="path">
            <h3>Kubernetes</h3>
            <p>
              A Helm chart with probes, HPA, and ServiceMonitor — plus fold-discovery, so labeled
              Services join the federation on their own.
            </p>
          </article>
        </div>
      </div>
    </section>

    <section
      class="problems"
      data-section="problems"
      data-intent="map problem to config"
      data-layout="grid"
      data-items="9"
    >
      <div class="wrap">
        <div class="section-head">
          <!-- Broken at the sentence, not wherever the box runs out: balance
               would otherwise leave "One gateway. A" hanging on line one.
               Both sentences fit a line on their own at every width. -->
          <h2 class="no-balance">One gateway.<br />A family of problems.</h2>
          <p class="intro">
            Most deployments start with one of these and grow into the others — they all ride the
            same config file.
          </p>
        </div>

        <div class="cluster">
          <div class="cluster-rail">
            <!-- glyph: three fold-lines converging to one -->
            <svg width="40" height="40" viewBox="0 0 40 40" aria-hidden="true">
              <path
                d="M 4 8 C 16 8, 16 20, 24 20 M 4 20 H 24 M 4 32 C 16 32, 16 20, 24 20"
                stroke="#6E6E6E"
                stroke-width="1.5"
                stroke-dasharray="4 3"
                fill="none"></path>
              <path d="M 24 20 H 36" stroke="#D6FF00" stroke-width="2" fill="none"></path>
              <path d="M 32 16 L 37 20 L 32 24 Z" fill="#D6FF00"></path>
            </svg>
            <h3>Federate</h3>
          </div>
          <div class="cluster-cards">
            <article class="problem">
              <h4>Unify a federation</h4>
              <p>
                Acquisitions, child orgs, and teams each ship their own MCP servers — any language,
                any SDK. fold presents them as one virtual server with namespaced tools. No team
                rewrites anything.
              </p>
            </article>
            <article class="problem">
              <h4>Discover upstreams automatically</h4>
              <p>
                A team ships an MCP server, the registry lists it, and it appears behind the gateway
                — no config change. On Kubernetes, label a Service fold.run/upstream: "true" and
                fold-discovery does the rest.
              </p>
            </article>
            <article class="problem">
              <h4>Run tasks across the federation</h4>
              <p>
                Long-running work flows through fold: task polls and updates route to the server
                that owns the task — remembered at mint, or found by probe — and tasks/list merges
                every org, scoped per principal.
              </p>
            </article>
          </div>
        </div>

        <div class="cluster">
          <div class="cluster-rail">
            <!-- glyph: route through a notched gate -->
            <svg width="40" height="40" viewBox="0 0 40 40" aria-hidden="true">
              <path d="M 4 20 H 16" stroke="#6E6E6E" stroke-width="1.5" stroke-dasharray="4 3" fill="none"
              ></path>
              <path d="M 18 10 v 20 M 23 12 v 16" stroke="#bcbcbc" stroke-width="1.5"></path>
              <path d="M 25 20 H 36" stroke="#D6FF00" stroke-width="2" fill="none"></path>
              <path d="M 32 16 L 37 20 L 32 24 Z" fill="#D6FF00"></path>
            </svg>
            <h3>Govern</h3>
          </div>
          <div class="cluster-cards">
            <article class="problem">
              <h4>Draw the security boundary</h4>
              <p>
                One choke point for authentication, deny-by-default tool allowlists, per-principal
                visibility, and an audit event for every request — including the denials.
              </p>
            </article>
            <article class="problem">
              <h4>Broker credentials</h4>
              <p>
                Clients hold one token with fold as audience; fold exchanges it per upstream (RFC
                8693) or injects service credentials. API keys never reach agents.
              </p>
            </article>
            <article class="problem">
              <h4>Govern vendor MCP servers</h4>
              <p>
                Put third-party and SaaS MCP endpoints behind your own auth, policy, and audit —
                instead of scattering per-user API keys across every client.
              </p>
            </article>
          </div>
        </div>

        <div class="cluster">
          <div class="cluster-rail">
            <!-- glyph: route with gauge ticks -->
            <svg width="40" height="40" viewBox="0 0 40 40" aria-hidden="true">
              <path d="M 4 20 H 36" stroke="#D6FF00" stroke-width="2" fill="none"></path>
              <path d="M 12 14 v 5 M 20 12 v 7 M 28 14 v 5" stroke="#6E6E6E" stroke-width="1.5"
              ></path>
              <path d="M 12 26 v -3 M 20 28 v -5 M 28 26 v -3" stroke="#6E6E6E" stroke-width="1.5"
              ></path>
            </svg>
            <h3>Operate</h3>
          </div>
          <div class="cluster-cards">
            <article class="problem">
              <h4>Protect fragile services</h4>
              <p>
                List caching, global and per-upstream rate limits, and circuit breakers stand
                between agent traffic storms and your internal systems.
              </p>
            </article>
            <article class="problem">
              <h4>Expose tools outward, carefully</h4>
              <p>
                Offer partners a curated, policy-scoped subset of internal tools on one hardened
                endpoint — a single static binary, self-hosted in your VPC.
              </p>
            </article>
            <article class="problem">
              <h4>Conformant, provably</h4>
              <p>
                The official MCP conformance suite runs through fold on every merge — 40/40 checks,
                including sampling, elicitation, and subscriptions bridged through the gateway.
              </p>
            </article>
          </div>
        </div>
      </div>
    </section>

    <section
      class="closing"
      data-section="closing"
      data-intent="drive action"
      data-layout="full-bleed"
      data-module="cta-band"
    >
      <div class="wrap">
        <h2>Start where it hurts.</h2>
        <p>
          A single passthrough upstream in front of your most-used server is one config file away.
          Governance grows from there.
        </p>
        <a class="btn btn-primary" href="https://docs.fold.run/getting-started/">Get started</a>
      </div>
    </section>
  </main>
</Base>

<script>
  /* copy buttons: progressive enhancement, no external deps */
  const live = document.createElement('span');
  live.setAttribute('role', 'status');
  live.style.cssText = 'position:absolute;left:-9999px';
  document.body.appendChild(live);
  for (const btn of document.querySelectorAll<HTMLButtonElement>('[data-copy]')) {
    btn.addEventListener('click', async () => {
      const pre = btn.closest('.code-panel')?.querySelector('pre');
      if (!pre) return;
      try {
        await navigator.clipboard.writeText(pre.innerText);
        btn.textContent = 'copied';
        live.textContent = 'Copied to clipboard';
      } catch {
        const range = document.createRange();
        range.selectNodeContents(pre);
        const sel = getSelection();
        if (sel) {
          sel.removeAllRanges();
          sel.addRange(range);
        }
        btn.textContent = 'press ⌘C';
        live.textContent = 'Clipboard unavailable; text selected, press Control or Command C';
      }
      setTimeout(() => {
        btn.textContent = 'copy';
      }, 1800);
    });
  }

  /* The hero answers to the pointer and to nothing else: one handler, throttled
     to a frame, publishing the cursor twice — normalised (--mx/--my) for the
     glow parallax, and in hero-local pixels (--cx/--cy) for the lit patch of
     grid. Everything downstream is CSS, so there is no loop running when the
     visitor is still. `is-tracking` gates the light on the same conditions, so
     a coarse pointer or a reduced-motion visitor never gets one with nothing to
     follow. */
  {
    const hero = document.querySelector<HTMLElement>('[data-glow]');
    const fine = matchMedia('(pointer: fine)');
    const calm = matchMedia('(prefers-reduced-motion: reduce)');
    if (hero && fine.matches && !calm.matches) {
      let raf = 0;
      hero.classList.add('is-tracking');
      hero.addEventListener('pointermove', (e: PointerEvent) => {
        if (raf) return;
        raf = requestAnimationFrame(() => {
          const r = hero.getBoundingClientRect();
          const x = e.clientX - r.left;
          const y = e.clientY - r.top;
          hero.style.setProperty('--mx', String(x / r.width - 0.5));
          hero.style.setProperty('--my', String(y / r.height - 0.5));
          hero.style.setProperty('--cx', `${x}px`);
          hero.style.setProperty('--cy', `${y}px`);
          raf = 0;
        });
      });
      hero.addEventListener('pointerleave', () => {
        hero.style.setProperty('--mx', '0');
        hero.style.setProperty('--my', '0');
      });
    }
  }
</script>

<style is:global>
  /* ===== hero ===== */
  .hero {
    position: relative;
    overflow: hidden;
  }
  .hero-field {
    position: absolute;
    inset: 0;
    pointer-events: none;
    transform: translate3d(calc(var(--mx, 0) * 28px), calc(var(--my, 0) * 20px), 0);
    transition: transform 0.35s cubic-bezier(0.22, 1, 0.36, 1);
  }
  /* The pattern-paper ground: a dark ruled sheet, and the same rules again in
     Live revealed only under the pointer. Both layers are plain CSS gradients,
     so the compositor carries them and there is no canvas and no frame loop
     behind the hero at all. Masked so the sheet dissolves before it reaches the
     copy on the left. */
  .hero-grid {
    /* Bleeds the sheet past the section's edges, so the light stays a full disc
       when the pointer is near a corner. It is also the offset --cx/--cy need,
       since those arrive hero-local. */
    --grid-bleed: 80px;
    position: absolute;
    inset: calc(-1 * var(--grid-bleed));
    pointer-events: none;
    mask-image: radial-gradient(112% 108% at 76% 28%, #000 24%, transparent 86%);
    -webkit-mask-image: radial-gradient(112% 108% at 76% 28%, #000 24%, transparent 86%);
  }
  .hero-rules {
    position: absolute;
    inset: 0;
    opacity: 0.7;
    background-image:
      linear-gradient(to right, var(--color-trace) 1px, transparent 1px),
      linear-gradient(to bottom, var(--color-trace) 1px, transparent 1px);
    background-size: 64px 64px;
  }
  /* Revealed in a soft disc under the pointer: the sheet lights where you are
     and goes dark again behind you. Sharing a parent and a background-size with
     .hero-rules is what keeps the lit rules exactly over the dark ones. */
  .hero-cursor {
    position: absolute;
    inset: 0;
    opacity: 0;
    transition: opacity 0.45s cubic-bezier(0.22, 1, 0.36, 1);
    background-image:
      radial-gradient(
        170px 170px at calc(var(--cx, 50%) + var(--grid-bleed))
          calc(var(--cy, 50%) + var(--grid-bleed)),
        color-mix(in srgb, var(--color-accent) 18%, transparent),
        transparent 70%
      ),
      linear-gradient(to right, var(--color-accent) 1px, transparent 1px),
      linear-gradient(to bottom, var(--color-accent) 1px, transparent 1px);
    background-size:
      100% 100%,
      64px 64px,
      64px 64px;
    mask-image: radial-gradient(
      220px 220px at calc(var(--cx, 50%) + var(--grid-bleed))
        calc(var(--cy, 50%) + var(--grid-bleed)),
      #000,
      transparent 72%
    );
    -webkit-mask-image: radial-gradient(
      220px 220px at calc(var(--cx, 50%) + var(--grid-bleed))
        calc(var(--cy, 50%) + var(--grid-bleed)),
      #000,
      transparent 72%
    );
  }
  /* `is-tracking` is set by the script, so a coarse pointer or a reduced-motion
     visitor never gets a light that has nothing to follow. */
  .hero.is-tracking:hover .hero-cursor {
    opacity: 0.8;
  }
  .glow {
    position: absolute;
    border-radius: 50%;
    filter: blur(90px);
  }
  .glow-a {
    width: 560px;
    height: 420px;
    right: -6%;
    top: 4%;
    background: radial-gradient(closest-side, rgba(214, 255, 0, 0.18), transparent 70%);
  }
  .glow-b {
    width: 420px;
    height: 360px;
    left: 30%;
    bottom: -22%;
    background: radial-gradient(closest-side, rgba(188, 188, 188, 0.1), transparent 70%);
  }
  @media (prefers-reduced-motion: no-preference) {
    .glow-a {
      animation: drift-a 14s ease-in-out infinite alternate;
    }
    .glow-b {
      animation: drift-b 18s ease-in-out infinite alternate;
    }
    @keyframes drift-a {
      to {
        transform: translate3d(-40px, 26px, 0);
      }
    }
    @keyframes drift-b {
      to {
        transform: translate3d(48px, -20px, 0);
      }
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .hero-field {
      transition: none;
      transform: none;
    }
    .hero-cursor {
      display: none;
    }
  }
  .hero .wrap {
    position: relative;
    display: grid;
    /* The copy column is sized to the headline rather than to a share of the
       row: it needs enough for "The enterprise / MCP gateway." on two lines and
       nothing more, and everything left over goes to the diagram. */
    grid-template-columns: minmax(0, 31rem) minmax(0, 1fr);
    gap: var(--spacing-xl);
    align-items: center;
  }
  .hero {
    padding-block: calc(var(--section-padding) + 16px);
  }
  .kicker {
    font-family: var(--mono-font-family);
    font-size: var(--body-sm);
    letter-spacing: var(--tracking-meta);
    font-variant-numeric: tabular-nums slashed-zero;
    color: var(--color-carrier);
    margin-bottom: var(--spacing-lg);
  }
  .kicker .sep {
    color: var(--color-fold-line);
    margin-inline: 0.7em;
  }
  .hero h1 {
    font-size: var(--heading-xxl);
    font-weight: var(--weight-heading);
    line-height: var(--line-height-heading);
    letter-spacing: var(--tracking-display);
    margin-bottom: var(--spacing-lg);
  }
  .hero .lede {
    margin-bottom: var(--spacing-xl);
    max-width: 46ch;
  }
  .cta-row {
    display: flex;
    gap: var(--spacing-md);
    flex-wrap: wrap;
  }
  /* No rule between the columns: the grid's own gap separates them, and the
     ruled sheet behind the diagram already gives that side its own ground. */
  .diagram-panel {
    min-width: 0;
  }
  .diagram-panel svg {
    width: 100%;
    height: auto;
    display: block;
  }

  /* topology diagram: fold-line vocabulary */
  .fold-dash {
    stroke: var(--color-fold-line);
    stroke-width: 1.5;
    stroke-dasharray: 5 5;
    fill: none;
  }
  .fold-active {
    stroke: var(--color-accent);
    stroke-width: 2;
    fill: none;
  }
  .fold-node {
    fill: var(--color-rack);
    stroke: var(--color-trace);
  }
  .fold-node-label {
    font-family: var(--mono-font-family);
    font-size: 16px;
    fill: var(--color-fg);
    letter-spacing: 0.02em;
  }
  .fold-anno {
    font-family: var(--mono-font-family);
    font-size: 13px;
    fill: var(--color-static);
    letter-spacing: 0.03em;
  }
  .fold-notch {
    stroke: var(--color-carrier);
    stroke-width: 1.5;
  }
  .fold-arrow {
    fill: var(--color-accent);
  }
  @media (prefers-reduced-motion: no-preference) {
    .fold-active.draw {
      stroke-dasharray: 720;
      stroke-dashoffset: 720;
      animation: fold-draw 1.6s cubic-bezier(0.22, 1, 0.36, 1) 0.3s forwards;
    }
    @keyframes fold-draw {
      to {
        stroke-dashoffset: 0;
      }
    }
  }

  /* ===== proof band ===== */
  .proof .wrap {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
    gap: var(--spacing-md);
    padding: 0;
    max-width: calc(var(--max-width) - 2.5rem);
  }
  .proof {
    padding-block: 40px;
  }
  .stat {
    background: var(--color-rack);
    border: 1px solid var(--color-trace);
    border-radius: var(--radius);
    position: relative;
    display: block;
    background: var(--color-bg);
    padding: var(--spacing-md) var(--spacing-lg) var(--spacing-xl);
    color: var(--color-fg);
  }
  .stat::after {
    content: '\2192';
    position: absolute;
    right: var(--spacing-lg);
    bottom: var(--spacing-md);
    color: var(--color-accent);
    font-size: 1.1rem;
  }
  .stat:hover {
    background: var(--color-rack);
    text-decoration: none;
  }
  /* The proof numerals. Mono, tabular and slashed-zero so "40/40" and
     "~0.2 ms" read as measurements rather than as marketing. */
  .stat b {
    font-family: var(--mono-font-family);
    font-size: 1.625rem;
    font-weight: var(--weight-numeral);
    font-variant-numeric: tabular-nums slashed-zero;
    display: block;
    margin-bottom: 8px;
    letter-spacing: -0.015em;
    line-height: 1.15;
  }
  /* The caveat under each numeral is a caption, so it stays at meta size. */
  .stat span {
    color: var(--color-static);
    font-size: var(--body-sm);
    line-height: var(--line-height-meta);
    display: block;
    max-width: 34ch;
  }

  /* Section head: the title hangs in a narrow left column and the standfirst
     runs beside it, so the head uses the full measure of the page instead of
     leaving the right half of every section empty. Baselines align on the
     first line, which is what makes the pairing look set rather than stacked. */
  .section-head {
    display: grid;
    grid-template-columns: minmax(0, 26rem) minmax(0, 1fr);
    gap: var(--spacing-md) var(--spacing-xl);
    align-items: baseline;
    margin-bottom: var(--spacing-xl);
  }
  .section-head h2 {
    margin: 0;
  }
  .no-balance {
    text-wrap: initial;
  }
  .section-head .intro {
    margin: 0;
  }
  @media (max-width: 900px) {
    .section-head {
      grid-template-columns: 1fr;
      gap: var(--spacing-md);
    }
  }
  .termini {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: var(--spacing-md);
  }
  .terminus {
    background: var(--color-rack);
    border: 1px solid var(--color-trace);
    border-radius: var(--radius);
    background: var(--color-rack);
    border: 1px solid var(--color-trace);
    border-radius: var(--radius-lg);
    padding: var(--spacing-lg);
    position: relative;
  }
  .terminus::before {
    /* fold-line connector notch: each terminus hangs off the shared route */
    content: '';
    position: absolute;
    top: -13px;
    left: var(--spacing-lg);
    width: 1px;
    height: 12px;
    border-left: 1px solid var(--color-trace);
  }
  .route-rail {
    margin-bottom: var(--spacing-md);
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
  }
  .route-rail .chip {
    font-family: var(--mono-font-family);
    font-size: var(--body-sm);
    letter-spacing: 0;
    color: var(--color-carrier);
    background: var(--color-rack);
    border: 1px solid var(--color-trace);
    border-radius: var(--radius-sm);
    padding: 3px 10px;
  }
  /* The namespace prefixes are literal wire strings, so they keep the mono —
     the one place a heading earns it. */
  .terminus h3 {
    font-family: var(--mono-font-family);
    font-size: 1.0625rem;
    font-weight: var(--weight-heading);
    letter-spacing: 0;
    line-height: var(--line-height-card);
    margin-bottom: var(--spacing-sm);
    color: var(--color-fg);
  }
  .terminus p {
    color: var(--color-static);
    font-size: var(--body);
    line-height: 1.6;
  }

  /* ===== run-it: numbered route ===== */
  .route-steps {
    display: grid;
    gap: var(--spacing-lg);
    list-style: none;
    padding: 0;
  }
  .step {
    display: grid;
    grid-template-columns: 44px 1fr;
    gap: var(--spacing-md);
  }
  .step > div:last-child {
    min-width: 0;
  }
  .step .marker {
    width: 32px;
    height: 32px;
    border-radius: 0;
    border: 1.5px solid var(--color-trace);
    display: grid;
    place-items: center;
    font-family: var(--mono-font-family);
    font-size: var(--body-sm);
    font-weight: 600;
    font-variant-numeric: tabular-nums;
    letter-spacing: 0;
    color: var(--color-carrier);
    position: relative;
    background: var(--color-bg);
    z-index: 1;
  }
  .step {
    position: relative;
  }
  .step:not(:last-child)::before {
    content: '';
    position: absolute;
    left: 15.5px;
    top: 36px;
    bottom: -24px;
    border-left: 1px solid var(--color-trace);
  }
  .step h3 {
    margin-bottom: var(--spacing-sm);
    font-size: var(--heading-md);
  }
  .step .muted {
    font-size: var(--body);
  }
  .paths {
    margin-top: var(--spacing-xl);
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: var(--spacing-md);
  }
  .path {
    background: var(--color-rack);
    border: 1px solid var(--color-trace);
    border-radius: var(--radius);
    padding: var(--spacing-lg);
  }
  .path h3 {
    font: var(--weight-heading) var(--heading-md) / var(--line-height-card)
      var(--heading-font-family);
    letter-spacing: var(--tracking-card);
    margin-bottom: var(--spacing-sm);
  }
  .path p {
    color: var(--color-static);
    font-size: var(--body);
    line-height: 1.6;
  }
  .path .cmd {
    display: block;
    margin-top: 10px;
    font-family: var(--mono-font-family);
    font-size: 0.8125rem;
    letter-spacing: 0;
    color: var(--color-carrier);
    overflow-wrap: anywhere;
  }


  /* ===== problems: 3 cluster bands ===== */
  .cluster {
    display: grid;
    grid-template-columns: 200px repeat(3, minmax(0, 1fr));
    gap: var(--spacing-md);
  }
  .cluster + .cluster {
    margin-top: var(--spacing-md);
  }
  .cluster-rail {
    background: var(--color-rack);
    border: 1px solid var(--color-trace);
    border-radius: var(--radius);
    background: var(--color-rack);
    padding: var(--spacing-lg);
  }
  .cluster-rail h3 {
    font-size: var(--heading-lg);
    letter-spacing: var(--tracking-title);
    margin-top: var(--spacing-md);
  }
  .cluster-rail svg {
    display: block;
  }
  .cluster-cards {
    display: contents;
  }
  .problem {
    background: var(--color-rack);
    border: 1px solid var(--color-trace);
    border-radius: var(--radius);
    padding: var(--spacing-lg);
  }
  .problem h4 {
    margin-bottom: 8px;
  }
  .problem p {
    color: var(--color-static);
    font-size: var(--body);
    line-height: 1.6;
  }

  /* ===== closing: the Route drench ===== */
  .closing :focus-visible {
    outline-color: var(--color-accent);
  }
  .closing {
    background: #121212;
    border-top: 1px solid var(--color-trace);
    
    padding-block: 80px;
  }
  /* The statement holds the left, the action sits at the end of the line.
     On the wide container a single left-aligned stack leaves half the band
     empty; this makes the width part of the composition. */
  .closing .wrap {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: var(--spacing-lg) var(--spacing-2xl);
    align-items: center;
  }
  .closing h2 {
    color: var(--color-fg);
    font-size: clamp(2rem, 2.2vw + 1.2rem, 2.75rem);
    max-width: 18ch;
  }
  .closing p {
    color: var(--color-carrier);
    font-size: var(--body-lg);
    line-height: 1.6;
    max-width: 52ch;
  }
  .closing .btn {
    grid-column: 2;
    grid-row: 1 / span 2;
    justify-self: end;
  }
  .closing .btn-primary {
    background: var(--color-accent);
    color: #121212;
  }
  .closing .btn-primary:hover {
    background: #b4d600;
  }

  /* ===== responsive (page-specific) ===== */
  @media (max-width: 1024px) {
    .hero .wrap {
      grid-template-columns: 1fr;
    }
    .terminus::before {
      display: none;
    }
    .diagram-panel {
      max-width: 560px;
    }
    .cluster {
      grid-template-columns: 1fr;
      gap: var(--spacing-md);
    }
    .cluster-rail {
      display: flex;
      align-items: center;
      gap: var(--spacing-md);
    }
    .cluster-rail h3 {
      margin-top: 0;
    }
  }
  @media (max-width: 840px) {
    .closing .wrap {
      grid-template-columns: 1fr;
      justify-items: start;
    }
    .closing .btn {
      grid-column: 1;
      grid-row: auto;
      justify-self: start;
    }
  }
  @media (max-width: 640px) {
    .cta-row .btn {
      flex: 1;
      justify-content: center;
    }
    .closing .btn {
      width: 100%;
      justify-content: center;
    }
    .step {
      grid-template-columns: 32px 1fr;
      gap: var(--spacing-sm);
    }
  }
</style>
