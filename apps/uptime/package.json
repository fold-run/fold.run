{
  "name": "@fold-run/uptime",
  "version": "0.0.1",
  "private": true,
  "description": "Cron uptime monitor for fold.run properties: HTTP checks on site and docs, webhook alerts on state transitions.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "typescript": "catalog:",
    "wrangler": "catalog:"
  },
  "license": "Apache-2.0"
}
